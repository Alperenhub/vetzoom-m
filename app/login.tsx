import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Login = () => {
  return (
    <View>
      <Text className="text-2xl text-red-500">Login</Text>
    </View>
  );
};

export default Login;

const styles = StyleSheet.create({});
